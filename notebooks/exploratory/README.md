# Exploratory Notebooks

Estos notebooks son demos y exploracion reproducible.

Reglas:

- deben correr desde la raiz del repo o detectar la raiz automaticamente;
- deben escribir salidas en `artifacts/outputs/`;
- deben evitar rutas absolutas y outputs innecesarios versionados.
